#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{

    char registro[4] = {'000'};
    // char c = '0';
    // memset(registro, 0, sizeof(registro));
    printf("%s\n", registro);
    return 0;
}